[providers]
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'build: ts-node runs source directly'"]

[start]
cmd = "node --max-old-space-size=4096 -r ts-node/register/transpile-only src/index.ts"
