[providers]
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx esbuild src/index.ts --bundle=false --platform=node --format=cjs --outfile=dist/index.js"]

[start]
cmd = "node --max-old-space-size=4096 dist/index.js"
