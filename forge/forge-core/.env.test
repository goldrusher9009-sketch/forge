# Test Environment Configuration for Forge Core Integration Tests

# Database for integration tests
# Point to a local PostgreSQL instance or Docker container
TEST_DATABASE_URL=postgres://postgres:postgres@localhost/forge_test

# Ensure migrations run successfully
SQLX_OFFLINE=false

# Test execution settings
RUST_LOG=forge_core=debug,sqlx=info
