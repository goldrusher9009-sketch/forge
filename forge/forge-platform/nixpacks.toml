[providers]
providers = ["node", "python"]

[phases.setup]
nixPkgs = ["python3", "make", "gcc", "nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'dist pre-built'"]

[phases.start]
cmd = "node --max-old-space-size=1024 dist/index.js"
